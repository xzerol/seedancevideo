# 2026-05-21 节点画布与删除能力

## 新增

- React Flow 节点画布。
- 人物角色库与素材库侧边栏。
- Seedream 5.0 图片生成接口。
- 项目、画布节点、连线、本地保存数据模型。
- 生成结果手动收藏到资源库。

## 本次补充

- 资源库卡片增加删除按钮。
- 画布素材/人物节点增加删除按钮。
- Seedream/Seedance 生成卡片增加删除按钮。
- 图片/视频结果卡片增加删除按钮。
- 删除画布节点时同步删除相关连线。
- 删除资源库素材时同步清理当前画布引用。

## 验证

- `npm run test`
- `npm run build`
- `docker compose up -d --build`
- 浏览器打开 `http://localhost:3001` 检查画布与样式。
